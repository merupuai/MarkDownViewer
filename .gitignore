# Dependencies
node_modules/

# Build artifacts
build/
artifacts/
dist/

# Git worktrees (isolated dev workspaces)
.worktrees/

# CoBolt / runtime instrumentation outputs (not part of the app)
_cobolt-output/
**/_cobolt-output/
.cobolt-backups/
.claude/

# Local AI tool caches (per-machine, not project state)
.codex/
.stitch/

# Logs
*.log
/tmp/mdv-*.log

# OS junk
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
.history/

# Bun
.bun/

# Electron / Electrobun cache
.electrobun-cache/
electrobun-core-*.tar.gz
core-macos-*-temp.tar.gz
core-win-*-temp.tar.gz
core-linux-*-temp.tar.gz

# Compiled native helpers (reproducible from sources/cocoa-launcher.swift)
build/tools/

# Local-only secrets
.env
.env.local
*.pem

# CoBolt user infrastructure config (contains secrets)
.env.cobolt
.env.mcp

# Project-local exclusions (per request)
_cobolt-docker/
.github/workflows/
.gitignore
design-tokens.json
tests/unit/
AGENTS.md
